# OpenSteamClient's Console UI
This is the UNDONE console UI for OpenSteamClient. Currently, it only runs the bootstrapper.
Eventually I want it to be a SteamCMD-style launcher, but more fancy (think htop)